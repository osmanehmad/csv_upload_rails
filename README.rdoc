Simple Rails application to import a CSV file and render it on the view layer

Uses mongodb at the back end, dynamic fields is set to false, which means:-
Mongo db will accept dynamic fields, you can upload any csv file, with any field names and values
the model is independent of field names


